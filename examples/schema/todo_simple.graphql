type Query {
    readTodos: [String!]!
    readTodo(id: ID!): String!
}

type Mutation {
    createTodo(todo: String!): String!
    updateTodo(id: ID!, todo: String!): String!
    deleteTodo(id: ID!): String!
}