type Todo {
    id: ID!
    text: String!
}

input TodoInput {
    text: String!
}

type Mutation {
    createTodo(todo: TodoInput!): Todo
    updateTodo(id: ID!, todo: TodoInput): Todo
    deleteTodo(id: ID!): Todo
}